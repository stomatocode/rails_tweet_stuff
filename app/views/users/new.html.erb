

<%= form_for(@user) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :password, "Password" %>
    <%= f.text_field :password %>
    <%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end %>
